(() => {
  const DATA_URL = "data/selected-research.xlsx";
  const SHEET_NAME = "Selected Research";
  const FALLBACK_ROWS = [
  {
    "Photo Filename": "Figure1.png",
    "Year": "2026",
    "Title": "High-resolution models better simulate historical snowpack declines in the Upper Colorado River Basin",
    "Author and Journal Details": "Dixit, A., Rahimi, S., Huang, L., Musselman, K. N., Vano, J. A., Addor, N., & Lehner, F. ",
    "Journal": "Environmental Research Letters, 21(5), 054012",
    "Paper URL": "https://doi.org/10.1088/1748-9326/ae4113",
    "Abstract": "The Colorado River supplies water to 40 million people, 4.5 million acres of irrigated land, seven US states, over two dozen federally recognized and sovereign tribes and Mexico. River discharge is strongly controlled by snowpack accumulation and melt in the headwater regions, making upper-basin snow dynamics a primary driver of water availability throughout the basin. Therefore, changes in snow water equivalent (SWE) are critical for water resources planning in the Upper Colorado River Basin (UCRB), yet there is considerable uncertainty even in historical SWE trends. We compare historical UCRB SWE trends across various gridded snow products, including new Weather Research and Forecasting simulations at 45, 9, and 3 km resolution, against snow telemetry station data. We find that SWE trends are systematically amplified with higher model resolution and that high resolution (<10 km) is needed to accurately capture not only SWE amounts but also historical SWE declines across high-elevation mountains. Trend amplification primarily results from higher resolution simulations having higher snowfall due to stronger orographic forcing, an effect further modulated by synoptics. This is confirmed in a set of alternative historical realizations from a downscaled climate model large ensemble, which, despite a tendency for SWE declines, highlight the important role of internal variability in historical UCRB SWE trends. These results demonstrate that model resolution plays an important role in shaping assessments of snow-related climate change, with implications for a wide range of impact studies.",
    "Display": "Yes"
  },
  {
    "Photo Filename": "Figure2.png",
    "Year": "2025",
    "Title": "Optimizing WRF-Hydro Calibration in the Himalayan Basin: Precipitation Influence and Parameter Sensitivity Analysis",
    "Author and Journal Details": "Dixit, A., Sahany, S., Mishra, S. K., & Lehner, F.",
    "Journal": "Journal of Hydrometeorology, 26(3), 345-363.",
    "Paper URL": "https://doi.org/10.1175/JHM-D-24-0096.1",
    "Abstract": "The lack of in situ observations and reliable climate information in large parts of the Himalayas poses significant challenges for assessing water resources vulnerability. Integrated hydrometeorological modeling systems, like WRF-Hydro, have the potential to provide information in such ungauged or poorly observed regions, yet they require careful calibration. Here, we calibrate and assess the fidelity of WRF-Hydro in simulating the hydrological regime of the Beas basin. Selected WRF-Hydro model parameters are calibrated using the parameter estimation (PEST) framework, leveraging eight WRF-Hydro simulations with two meteorological forcings from two WRF realizations. Model calibration improves the accuracy of the basin discharge simulation; however, the choice of precipitation forcing is also found to be critically important. The performance of a given WRF-Hydro configuration varies with seasons. We thus propose an ensemble-weighting scheme to optimize an intra-annual trade-off between streamflow under- and overestimation in different WRF-Hydro configurations. This study demonstrates the efficacy of using WRF and WRF-Hydro for providing climate change impact–relevant information in data-sparse basins.",
    "Display": "Yes"
  },
  {
    "Photo Filename": "Figure3.png",
    "Year": "2023",
    "Title": "Seasonal dependent suitability of physical parameterizations to simulate precipitation over the Himalayan headwater",
    "Author and Journal Details": "Dixit, A., Sahany, S., Mishra, S. K., & Mesquita, M. D.",
    "Journal": "Scientific Reports, 13(1), 4756",
    "Paper URL": "https://doi.org/10.1038/s41598-023-31353-w",
    "Abstract": "The Himalayan ecosystem is fragile and needs robust management strategies for sustainability of natural resources such as water and vegetation. Therefore, reliable precipitation estimation becomes quite important from operational and regulation standpoints. It is crucial for numerous activities including policy/planning, agriculture, reservoir operations, disaster management, and others. In addition, reliable information on temporal variability of precipitation is also crucial for various applications such as agricultural and hydrological. The western Himalaya receives two distinct weather systems during summer and winter. Summer is responsible (largely) for rainfall and winter is for snowfall. Therefore, we hypothesize that there may not be a single set of parameterization schemes that can represent well both the weather systems. To investigate, we set up the WRF modeling system and performed six experiments with a combination of three microphysics (MP3, MP3, and WSM6) and two cumulus schemes (KF, and BMJ). It was found that the precipitation along the Himalayan foothills (near to basin terminal) is underestimated in four out of six experiments. Only experiments with BMJ cumulus scheme along with WSM6 and MP8 microphysics were able to show a considerable amount of precipitation along these foothills. It was noted that all six experiments showed high precipitation in the upstream region and over the mountain peaks and ridges in North-Western Himalaya. For DJF, each experiment was found to have large biases and none of them represented the observation with high confidence. However, the selection of observation reference data itself is a challenging task because of data paucity in this region. Therefore, the closest experiment to the most appropriate observation was selected as the reliable configuration (MP8_KF: MP8 microphysics and KF cumulus scheme) for DJF precipitation simulation. In this study we have, for the first time, reported the role of seasonal sensitivity for the climate scale simulations as we found that different schemes were suitable for different weather systems.",
    "Display": "Yes"
  },
  {
    "Photo Filename": "Figure4.png",
    "Year": "2023",
    "Title": "Modeling the Climate Change Impact on Hydroclimate Fluxes over the Beas Basin using a High-Resolution Glacier-Atmosphere-Hydrology Coupled Setup",
    "Author and Journal Details": "Dixit, A., Sahany, S., & Mishra, S. K. ",
    "Journal": "Journal of Hydrology, 627, 130219.",
    "Paper URL": "https://doi.org/10.1016/j.jhydrol.2023.130219",
    "Abstract": "Rapidly rising temperature is posing a high vulnerability towards stored water resources in the Himalayan region. However, different regions may have different response to global warming and, per se, different consequences. In this study, we coupled Weather Research & Forecasting Model (WRF) and WRF-Hydro to estimate present and future hydroclimate fluxes over the Beas basin to understand ongoing and projected impacts of climate change. WRF simulations are carried out at convection-permitting scale to produce high resolution atmospheric forcing that are fed to a calibrated WRF-Hydro to produce the future projection of hydrological fluxes.  In our results, no large difference in the amount of precipitation but contrasting pattern is noted. Higher reaches of the baisn are expected to experience lesser precipitation in future under both RCP4.5 and RCP8.5 scenarios (RCP: Representative Concentration Pathway). Contrastingly, downstream reaches and valley side region is expected to be wetter under these scenarios. Notably, the contrasting pattern is a composite effect of differential response of precipitation during different seasons (JJAS and DJF) under the changing climate. More so this region is expected to be warmer by 1–3 °C under RCP4.5 and 3–4 °C under RCP8.5 for JJAS at the end-21st-century. For DJF, 2.5–3.5 °C warming under RCP4.5 and 4.5–5 °C warming under RCP8.5 at end-21st-century is expected. In our results, no large difference in the amount of precipitation but contrasting pattern is noted. Higher reaches of the baisn are expected to experience lesser precipitation in future under both RCP4.5 and RCP8.5 scenarios (RCP: Representative Concentration Pathway). Contrastingly, downstream reaches and valley side region is expected to be wetter under these scenarios. Notably, the contrasting pattern is a composite effect of differential response of precipitation during different seasons (JJAS and DJF) under the changing climate. More so this region is expected to be warmer by 1–3 °C under RCP4.5 and 3–4 °C under RCP8.5 for JJAS at the end-21st-century. For DJF, 2.5–3.5 °C warming under RCP4.5 and 4.5–5 °C warming under RCP8.5 at end-21st-century is expected. Surface runoff is expected to decrease almost throughout the basin except over some regions of higher elevation during early-21st-century under RCP4.5 and during mid-21st-century under RCP8.5. The largest decline is found at end-21st-century under both RCP4.5 and RCP8.5 over high altitude regions. Subsurface flow (UGDRNOFF) decreased consistently under both RCP4.5 and RCP8.5, with a stronger declining signal under RCP8.5. UGDRNOFF is expected to decrease by ∼60 % under RCP4.5 and ∼70 % under RCP8.5 at the end of 21st century. Under RCP4.5, snow water equivalent (SNEQV) decreased for the regions having altitude <5 km, however, is found to be increased for the higher elevated regions. A similar response is expected under RCP8.5 until mid-21st–century. But, towards the end-21st-century, SNEQV is expected to decline throughout the year across the region. The highest decline in SNEQV is found to be occurred during the end-21st-century for both RCP4.5 and RCP8.5 scenarios.",
    "Display": "Yes"
  }
];
  const container = document.getElementById("selectedResearch");
  if (!container) return;

  const escapeHTML = (value = "") => String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");

  function createLightbox() {
    let box = document.getElementById("publicationLightbox");
    if (box) return box;
    box = document.createElement("div");
    box.id = "publicationLightbox";
    box.className = "publication-lightbox";
    box.hidden = true;
    box.innerHTML = `
      <button class="publication-lightbox-close" type="button" aria-label="Close image">×</button>
      <img class="publication-lightbox-image" alt="Expanded publication figure">
    `;
    document.body.appendChild(box);
    const close = () => {
      box.hidden = true;
      document.body.classList.remove("lightbox-open");
      box.querySelector("img").removeAttribute("src");
    };
    box.querySelector("button").addEventListener("click", close);
    box.addEventListener("click", e => { if (e.target === box) close(); });
    document.addEventListener("keydown", e => { if (e.key === "Escape" && !box.hidden) close(); });
    box._close = close;
    return box;
  }

  function render(rows) {
    const records = rows.filter(row => String(row.Display || "Yes").trim().toLowerCase() === "yes" && (row.Title || row.Year || row["Photo Filename"]));
    if (!records.length) {
      container.innerHTML = '<div class="publications-message">No selected research entries found.</div>';
      return;
    }

    container.innerHTML = records.map((row, i) => {
      const filename = String(row["Photo Filename"] || "").trim();
      const authors = row["Author and Journal Details"] || row.Authors || "";
      const journal = row.Journal || "";
      const paperURL = row["Paper URL"] || row.URL || "";
      const abstract = row.Abstract || "";
      const panelId = `selected-abstract-${i}`;

      const image = filename
        ? `<div class="publication-image-div">
             <img class="publication-image" src="${escapeHTML(filename)}" data-zoom-image tabindex="0" alt="Figure for ${escapeHTML(row.Title || "selected publication")}" loading="lazy">
           </div>`
        : `<div class="publication-image-div image-missing"><span class="publication-image-placeholder">No image</span></div>`;

      const citation = [
        authors ? `<span class="publication-authors">${escapeHTML(authors)}</span>` : "",
        journal ? `<em class="publication-journal">${escapeHTML(journal)}</em>` : ""
      ].filter(Boolean).join(authors && journal ? " · " : "");

      return `<article class="selected-publication">
        <div class="publication-media">${image}</div>
        <div class="publication-main">
          <div class="publication-year">${escapeHTML(row.Year || "")}</div>
          <h3>${escapeHTML(row.Title || "")}</h3>
          ${citation ? `<div class="publication-citation">${citation}</div>` : ""}
          <div class="publication-actions">
            ${abstract ? `<button class="abstract-toggle" type="button" aria-expanded="false" aria-controls="${panelId}">Abstract <span aria-hidden="true">↓</span></button>` : `<span></span>`}
            ${paperURL ? `<a class="paper-link" href="${escapeHTML(paperURL)}" target="_blank" rel="noopener">Paper →</a>` : ""}
          </div>
          ${abstract ? `<div class="abstract-panel" id="${panelId}" hidden><div class="abstract-inner"><p>${escapeHTML(abstract)}</p></div></div>` : ""}
        </div>
      </article>`;
    }).join("");

    const lightbox = createLightbox();
    const open = img => {
      const preview = lightbox.querySelector(".publication-lightbox-image");
      preview.src = img.currentSrc || img.src;
      lightbox.hidden = false;
      document.body.classList.add("lightbox-open");
      lightbox.querySelector("button").focus();
    };

    container.querySelectorAll("[data-zoom-image]").forEach(img => {
      img.addEventListener("click", () => open(img));
      img.addEventListener("keydown", e => {
        if (e.key === "Enter" || e.key === " ") { e.preventDefault(); open(img); }
      });
      img.addEventListener("error", () => {
        const holder = img.closest(".publication-image-div");
        holder.classList.add("image-missing");
        holder.innerHTML = `<span class="publication-image-placeholder">Image not found: ${escapeHTML(img.getAttribute("src") || "")}</span>`;
      }, {once:true});
    });

    container.querySelectorAll(".abstract-toggle").forEach(button => {
      button.addEventListener("click", () => {
        const panel = document.getElementById(button.getAttribute("aria-controls"));
        if (!panel) return;
        const opening = panel.hidden;
        panel.hidden = !opening;
        button.setAttribute("aria-expanded", String(opening));
        button.innerHTML = opening ? 'Abstract <span aria-hidden="true">↑</span>' : 'Abstract <span aria-hidden="true">↓</span>';
      });
    });
  }

  async function load() {
    try {
      if (typeof XLSX === "undefined") throw new Error("Spreadsheet reader did not load.");
      const response = await fetch(`${DATA_URL}?v=10`, { cache: "no-store" });
      if (!response.ok) throw new Error(`Could not load ${DATA_URL} (${response.status}).`);
      const workbook = XLSX.read(await response.arrayBuffer(), { type: "array" });
      const sheet = workbook.Sheets[SHEET_NAME] || workbook.Sheets[workbook.SheetNames[0]];
      if (!sheet) throw new Error("Selected research worksheet was not found.");
      render(XLSX.utils.sheet_to_json(sheet, { defval: "", raw: false }));
    } catch (error) {
      console.warn("Using embedded Selected Research fallback:", error);
      render(FALLBACK_ROWS);
    }
  }

  load();
})();
